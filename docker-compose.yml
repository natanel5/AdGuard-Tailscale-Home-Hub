# docker-compose.yml
version: "3.8"

services:
  # AdGuard Home: The primary DNS filter
  adguardhome:
    image: adguard/adguardhome:latest
    container_name: adguardhome
    restart: unless-stopped
    network_mode: host # Required for proper network visibility on Pi
    volumes:
      - ./adguard/conf:/opt/adguardhome/conf
      - ./adguard/work:/opt/adguardhome/work
  #
  # # Unbound: The recursive DNS resolver for privacy
  # unbound:
  #   image: mvance/unbound:latest
  #   container_name: unbound
  #   restart: unless-stopped
  #   ports:
  #     - "5335:53/udp"
  #     - "5335:53/tcp"
  #   volumes:
  #     - ./unbound/unbound.conf:/opt/unbound/etc/unbound/unbound.conf

  # # Tailscale: Secure remote access & Exit Node
  # tailscale:
  #   image: tailscale/tailscale:latest
  #   container_name: tailscale
  #   restart: unless-stopped
  #   network_mode: host
  #   cap_add:
  #     - NET_ADMIN
  #     - SYS_MODULE
  #   volumes:
  #     - ./tailscale/state:/var/lib/tailscale
  #     - /dev/net/tun:/dev/net/tun
  #   environment:
  #     - TS_STATE_DIR=/var/lib/tailscale
  #     - TS_USERSPACE=false
